# Routes @fortawesome Pro packages to FontAwesome's private registry. Not a
# secret by itself — the actual auth token must be set per-developer in their
# global ~/.npmrc (never commit it here). See README.md.
@fortawesome:registry=https://npm.fontawesome.com/
